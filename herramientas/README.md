# Herramientas

Se prevén `datos_iniciales.py`, `reiniciar_base_datos.py` y `crear_administrador.py`. Solo deben implementarse después de definir los modelos y la configuración de base de datos. La herramienta para reiniciar la base de datos deberá protegerse contra ejecuciones accidentales fuera del entorno local.
