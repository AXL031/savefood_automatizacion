# Scripts de apoyo

Se prevén `seed.py`, `reset_db.py` y `create_admin.py`. Solo deben implementarse después de definir los modelos y la configuración de base de datos. `reset_db.py` deberá protegerse contra ejecución accidental en entornos no locales.
