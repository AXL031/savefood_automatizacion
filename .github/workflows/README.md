# Flujos de integración continua

Se prevén `servidor-ci.yml`, `interfaz-ci.yml` e `integracion-ci.yml`. Se añadirán cuando existan instalaciones, comandos de análisis estático, pruebas y compilaciones ejecutables para evitar una falsa señal de calidad.
