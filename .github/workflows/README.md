# Workflows de CI

Se prevén `backend-ci.yml`, `frontend-ci.yml` e `integration-ci.yml`. Se añadirán cuando existan instalaciones, comandos de lint, pruebas y builds ejecutables para evitar pipelines que den una falsa señal de calidad.
